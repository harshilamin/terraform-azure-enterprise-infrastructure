# DEV Environment

This directory will contain the DEV Terraform root module.

Planned files: `backend.tf`, `main.tf`, `providers.tf`, `variables.tf`, `outputs.tf`, `terraform.tfvars.example`, and `versions.tf`.
