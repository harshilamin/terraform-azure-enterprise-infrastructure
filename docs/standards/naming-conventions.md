# Naming Conventions

Pattern:

```text
<resource-type>-<workload>-<environment>-<region>-<sequence>
```

Example:

```text
rg-platform-prod-centralus-001
```

Common abbreviations: `rg`, `vnet`, `snet`, `nsg`, `rt`, `kv`, `st`, `law`, `acr`, `aks`, `id`.
