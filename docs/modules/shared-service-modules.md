# Shared Service Modules

Implemented modules:

- `managed-identity`
- `key-vault`
- `storage-account`
- `container-registry`
- `log-analytics`

Each module is intentionally focused and exposes only operationally useful outputs.
