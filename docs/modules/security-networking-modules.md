# Security and Networking Modules

## route-table

Creates a route table, optional standalone routes, and subnet associations.

## private-dns-zone

Creates a private DNS zone and VNet links.

## private-endpoint

Creates a Private Link endpoint and private DNS zone group.

## management-lock

Creates `CanNotDelete` or `ReadOnly` management locks.
