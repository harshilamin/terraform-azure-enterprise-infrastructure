# Networking Modules

- `virtual-network`: creates a VNet
- `subnet`: creates a subnet and service endpoints
- `network-security-group`: creates and associates an NSG
- `vnet-peering`: creates one-way VNet peering
