# Resource Group Example

This example combines the naming and resource-group modules.

Run:

```bash
terraform init
terraform plan
```

Azure authentication is required only for a real plan or apply.
