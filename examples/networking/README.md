# Networking Example

This example creates:

- One resource group
- One virtual network
- One application subnet

Run:

```bash
terraform init
terraform plan
```

Use sanitized placeholder values only.
