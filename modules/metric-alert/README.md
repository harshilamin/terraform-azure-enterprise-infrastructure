# Metric Alert Module

Creates a static-threshold Azure Monitor metric alert.

The metric namespace and metric name are supplied by the calling environment,
allowing the module to support multiple Azure resource types.
