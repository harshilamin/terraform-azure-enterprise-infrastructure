# Action Group Module

Creates an Azure Monitor action group with zero or more email receivers.

Email addresses should be supplied through environment-specific variables or secure CI configuration rather than hard-coded in reusable modules.
